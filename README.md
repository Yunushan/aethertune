<h1 align="center">AetherTune</h1>

<p align="center">
  <strong>Free, open-source music workspace for mobile and desktop with local playback, open providers, playlists, lyrics, offline tools, and optional authenticated self-hosted library sync.</strong>
</p>

<p align="center">
  <img alt="license 0BSD" src="https://img.shields.io/badge/license-0BSD-blue">
  <img alt="status alpha" src="https://img.shields.io/badge/status-alpha-orange">
  <img alt="framework Flutter" src="https://img.shields.io/badge/flutter-Android%20%7C%20iOS%20%7C%20Desktop-02569B">
  <img alt="free open source" src="https://img.shields.io/badge/100%25-free%20%26%20open%20source-brightgreen">
  <img alt="privacy" src="https://img.shields.io/badge/telemetry-none-success">
</p>

<p align="center">
  <code>Dart 3</code> · <code>Flutter</code> · <code>Android</code> · <code>iOS</code> · <code>Linux</code> · <code>macOS</code> · <code>Windows</code> · <code>Server</code> · <code>0BSD</code>
</p>

<p align="center">
  <a href="#quick-start">Quick Start</a> ·
  <a href="#what-is-aethertune">Overview</a> ·
  <a href="#platform-preview">Platform Preview</a> ·
  <a href="#implemented-now">Implemented Now</a> ·
  <a href="#feature-goal">Feature Goal</a> ·
  <a href="docs/ARCHITECTURE.md">Architecture</a> ·
  <a href="docs/USER_GUIDE.md">User Guide</a> ·
  <a href="docs/RELEASE_GUIDE.md">Release Guide</a> ·
  <a href="LICENSE">License</a>
</p>

<p align="center">
  <a href="docs/media/readme/aethertune-platform-overview.svg">
    <img src="docs/media/readme/aethertune-platform-overview.svg" alt="AetherTune overview picture for Android, iOS, Windows, macOS, Linux, and the Dart server" width="900">
  </a>
</p>

<p align="center">
  <a href="docs/media/readme/aethertune-platform-tour.gif">
    <img src="docs/media/readme/aethertune-platform-tour.gif" alt="AetherTune animated GIF tour showing Android, iOS, Windows, macOS, Linux, and server surfaces" width="860">
  </a>
</p>

<p align="center">
  <sub>Illustrated, repo-hosted product previews for Android, iOS, Windows, macOS, Linux, and the Dart sync server. Select any image to view it at full size.</sub>
</p>

---

## What is AetherTune?

**AetherTune** is a GitHub-ready project for a completely free and open-source music app, starting with a Flutter client targeting **Android, iOS, Linux, macOS, and Windows** plus a Dart server service. The project is designed to combine the best product ideas from apps such as Kreate, OpenTune, InnerTune, SimpMusic, ArchiveTune, Spotube, Echo Music, Namida, PipePipe, NewPipe, LibreTube, Musify, AuraMusic, Bloomee Tunes, Gyawun Music, Music You, Muzza, SoundPod, NouTube, Grayjay, OuterTune, ViTune, RiMusic, Harmony Music, YMusic, YouTube Music, Flow, and MetroList.

The app is intentionally **provider-based**: the player, library, queue, cache, favorites, playlists, search, and UI are open-source core features, while source adapters can be added for legal sources such as local files, self-hosted servers, Internet Archive, Radio Browser, Jellyfin, Navidrome/Subsonic, podcasts, or other official APIs.

Playback failures are handled locally: AetherTune skips to the next playable queue item by default, stops after every playable item in a looped queue has failed, and lets the listener disable that recovery in Options.

Saved tracks can also contain listener-managed timestamp ranges that the player skips during normal playback. These ranges are edited in Now Playing, can be imported from or exported to bounded UTF-8 `.txt` files, persist through backup and sync, and never require an external segment feed or telemetry; active A-B repeat takes precedence.

> Important: this repository does **not** include DRM bypass, paid-service cloning, credential stealing, private API scraping, or any code intended to violate a platform's terms. You can build legal source adapters through the provider interface.

## Platform Preview

These generated product illustrations introduce each supported surface before someone builds the project locally; they are not device screenshots. The Flutter client targets Android, iOS, Linux, macOS, and Windows. CI compiles every target, the release workflow packages Android and desktop artifacts, and the optional Dart server provides authenticated, versioned library snapshots. The animated tour above is generated by `scripts/dev/render_readme_tour.py`; all checked-in README media lives in `docs/media/readme/` so GitHub does not depend on an external image host.

| Surface | What the README media introduces |
|---|---|
| Android | Touch-first library search, provider search, start-radio queues, and responsive mini/full player |
| iOS | Playlists, synced lyrics, history, and privacy-first sources |
| Windows | Wide desktop layout with provider search, radio queues, and playback controls |
| macOS | Artwork-first player, lyrics, playlists, and provider privacy |
| Linux | Local library, folders, open providers, backup, and server-friendly foundations |
| Server | Health/info/catalog APIs, per-device managed bearer tokens, and authenticated checksum-verified library snapshot sync |

### Mobile previews

| Android | iOS |
|---|---|
| <a href="docs/media/readme/aethertune-android.svg"><img src="docs/media/readme/aethertune-android.svg" alt="AetherTune Android preview with library search, provider search, start radio, and mini player" width="300"></a> | <a href="docs/media/readme/aethertune-ios.svg"><img src="docs/media/readme/aethertune-ios.svg" alt="AetherTune iOS preview with playlists, synced lyrics, and privacy-first providers" width="300"></a> |
| Library search, provider search, local playback, start radio, and mini player | Playlists, synced lyrics, privacy notes, and iOS/iPadOS-ready layout |

### Desktop previews

| Windows | macOS | Linux |
|---|---|---|
| <a href="docs/media/readme/aethertune-windows.svg"><img src="docs/media/readme/aethertune-windows.svg" alt="AetherTune Windows desktop preview with unified provider search, radio queues, and queue controls" width="300"></a> | <a href="docs/media/readme/aethertune-macos.svg"><img src="docs/media/readme/aethertune-macos.svg" alt="AetherTune macOS desktop preview with artwork, lyrics, and provider privacy" width="300"></a> | <a href="docs/media/readme/aethertune-linux.svg"><img src="docs/media/readme/aethertune-linux.svg" alt="AetherTune Linux desktop preview with local library, folders, providers, and server foundations" width="300"></a> |
| Unified search, radio queue, and mini player on a wide desktop surface | Artwork, lyrics, smart playlists, and provider privacy | Local library, folders, backup, providers, and Linux CI coverage |

### Server preview

<p align="center">
  <a href="docs/media/readme/aethertune-server.svg">
    <img src="docs/media/readme/aethertune-server.svg" alt="AetherTune server preview with health, info, catalog, provider, and sync API foundations" width="560">
  </a>
</p>

The server preview covers the Dart service that ships with the repo: health checks, API info, catalog endpoints, operator-configured bearer authentication, and durable per-user library snapshots with optimistic conflict detection. See [`services/server/README.md`](services/server/README.md) before exposing it to a network.

## Quick Start

The bootstrap requires Flutter and Python 3. Python applies and validates the
generated Android/iOS/macOS media-session and secure-storage settings.

```bash
git clone https://github.com/YOUR_NAME/aethertune.git
cd aethertune

# Creates mobile and desktop platform wrappers when they are not already generated.
./scripts/bootstrap_client.sh

cd apps/mobile
flutter pub get
flutter run
```

To run the server:

```bash
cd services/server
dart pub get
dart run bin/server.dart
```

To build release packages:

```bash
cd apps/mobile
flutter build apk --release
flutter build appbundle --release
flutter build ios --release
flutter build linux --release
flutter build macos --release
flutter build windows --release
```

## Implemented Now

This scaffold includes real app code, not only a README:

| Area | Implemented in this project |
|---|---|
| Mobile app | Flutter app shell for Android and iOS |
| Desktop app | Same Flutter client builds for Linux, macOS, and Windows in CI, with a desktop-width navigation rail |
| Server | Dart HTTP service with health/info/catalog endpoints, durable account profiles, one-time per-device token issue/rotation/revocation plus rate-limited last-authentication metadata through operations-authenticated APIs, authenticated `GET`/`PUT`/`DELETE /api/v1/sync/library`, and private shared-playlist APIs with checksums, size limits, and optimistic revision conflicts |
| Cross-device sync | Options can test and securely save a sync server/token, validate and display the authenticated account plus current managed device identity when the server supports it, refresh that non-secret identity without exposing the token, upload a portable library snapshot, download it with destructive confirmation, and explicitly resolve stale-revision conflicts by choosing the server or this device; snapshots carry saved history views and **Merge both** preserves independent views by ID. Optional automatic uploads run every 15 minutes while the app is foregrounded, skip unchanged snapshots when the server supports metadata, and never download or overwrite automatically; snapshots exclude credentials, device-local paths, private cache files/jobs, and device cache/offline settings, while downloads reattach matching local files |
| Private shared playlists | Authenticated users can host a private playlist, invite separately authenticated viewers or editors with opaque one-time codes that expire after seven days, invalidate unused codes before issuing replacements, join it as a linked local playlist, inspect its latest 25 revisions and restore an earlier one as a new revision, manually refresh/publish or explicitly merge local additions with the latest server order, revoke a collaborator, unlink locally, and delete a hosted server playlist. Shared payloads are limited to a name and portable library IDs: no media URLs, local paths, credentials, artwork, playback state, or automatic upload. Revision conflicts require an explicit refresh or merge choice. |
| Playback | Responsive mini/full Now Playing surfaces with artwork swipe navigation, seek/time labels, favorite, queue, lyrics, shuffle/repeat, foldered named timestamp bookmarks for saved tracks with an in-player bulk manager and bounded cross-device deletion, and transport controls; native playlist-backed `just_audio` controller for gapless local/URL queues plus optional 1/2/3/5/8-second crossfade overlaps for ordered known-duration tracks; persistent independent 0.5x-2x pitch shifts and device-local current-track pitch overrides on Android/Linux/Windows; Android device-band equalizer with adaptive Flat/Bass boost/Vocal/Treble/custom profiles and native 0-12 dB volume enhancement mirrored across crossfade; Android notification controls plus static and user-pinned Previous/Play-Pause/Next launcher shortcuts; iOS/macOS Control Center metadata and transport controls; Linux MPRIS and Windows SMTC metadata/timeline/transport sessions; configured mobile music sessions and background-audio wrappers; native Android/iOS/macOS playback backends; and bundled MediaKit audio backends for Linux/Windows |
| Local library | Import audio files through the native file picker or recursive folder scanner with filename metadata parsing plus basic ID3v1/ID3v2 MP3, FLAC Vorbis comment, Ogg/Opus comment, M4A metadata atom, and WAV RIFF INFO parsing, matching `.txt`/`.lrc`/`.srt`/`.ttml` lyric sidecars, plus embedded MP3/FLAC/M4A artwork display; edit saved metadata and transactionally write MP3/FLAC/Ogg/Opus/WAV tags plus standard M4A tags and PNG/JPEG cover art, repairing validated front-loaded-M4A chunk offsets; Ogg/Opus writes deliberately accept standard isolated comment pages and retain custom comments/media pages; add private file or web artwork for local tracks; resolve duplicates; search, sort, suggestion chips, and browse by artist, album, genre, source, flat folder groups, or recursive folder tree; artist and album groups open responsive full-screen pages with artwork, stats, play/shuffle, collection-aware radio, playlist saving, follow/view-artist navigation, and explainable related local collections |
| Persistence | Saves imported tracks, favorites, locally followed artists, playlists, lyrics, portable track bookmarks, podcast feed subscriptions, podcast refresh status, podcast episode progress, playback history, submitted search history, pause-listening-history and recommendation-signal preferences, app theme/accent and listening-recap visual theme preferences, offline mode, app/provider offline cache size limits, and the offline cache/download queue with byte-count/checksum metadata in `shared_preferences`; self-hosted provider secrets are excluded, stored through the platform credential vault, and never included in portable server-configuration exports |
| Backup/restore | Save or restore a versioned UTF-8 JSON backup through the native file dialog, or view/paste the JSON fallback, including locally followed artists, portable track bookmarks, submitted search history, pause-listening-history and recommendation-signal preferences, app theme/accent and listening-recap visual theme preferences, offline mode, app/provider offline cache size limits, and queued offline media requests with cache metadata, from the Options tab |
| Home feed | Local Home tab sections for explainable, user-configurable recommendations, playable/savable full-list mood/activity mixes, continue listening, recently played, newest local additions from followed artists, radio seeds, most played, favorites, recently added tracks, and range-filtered local charts with responsive top-track/top-artist bars. An explicit MusicBrainz release refresh can show dated album/EP/single metadata for up to four locally followed artists, with no media resolution, account, background polling, or offline request. An explicit **From your servers** refresh loads Jellyfin recently added albums, Navidrome/Subsonic newest/frequently played/recently played/random albums, and provider playlists. When a server's bounded recently-added shelf includes an artist the user follows, Home also surfaces a dedicated followed-artist shelf. Navidrome/Subsonic shelves offer explicit, deduplicated **Load more** continuation when their server-defined ordering has another page; other catalogs retain bounded album/playlist fallback and Home makes no automatic or offline requests |
| Search | Typo-tolerant local library filtering by title, artist, album, genre, source, folder, saved lyrics, favorites, and local-files-only offline readiness with sortable results and suggestion chips from submitted searches, playback history, and library metadata; Sources adds debounced, capability-gated Jellyfin and Navidrome/Subsonic artist/album/track type-ahead plus bounded Radio Browser station and Internet Archive item suggestions that launch the normal unified search and remain off in offline mode |
| Queue | Play the current list through one lazy gapless native playlist with automatic transition tracking, persistent next/previous/shuffle/repeat state, position-preserving reorder/remove controls, local track and artist/album radio, similar tracks, privacy-safe sharing, save-as-playlist, and up to twelve durable named queues that can be created, switched, renamed, and deleted independently |
| History/stats | Recently played tab with local playback history, typo-tolerant history search, persisted named range/query views, per-play entry deletion, play counts, responsive top-track/top-artist charts, monthly/yearly recap cards with four persisted visual themes and cross-platform PNG export, date ranges, JSON/CSV stats export, top tracks/artists/albums/genres, clear action, a persisted pause-listening-history privacy toggle, optional user-token ListenBrainz scrobbling after the service's completion threshold, and a user-confirmed import of the 100 most recent ListenBrainz events that only adds exact unambiguous matches to existing tracks |
| Playlists | Built-in and custom rule smart playlists with persisted Match all/Match any criteria and recursively nested rule groups, plus create, rename, folder organization/filtering, non-destructive pan/zoom crop positioning for explicit manual artwork, artwork URL edit, private collaboration, delete, open, find within, reorder, import/export JSON/M3U/CSV, copy playlist share text, and play manual playlists |
| Lyrics | Add, import, export, edit, view, search, translate, and delete plain text, LRC timestamped, SRT cue-timed, or TTML karaoke lyrics, including user-triggered LRCLIB plain/synced search with local attributed caching, optional on-demand translation through a user-configured LibreTranslate-compatible endpoint, matching folder-scan sidecars, playback-linked line/word highlighting, and attributed copyable excerpts with a bounded selected-line picker |
| Favorites | Toggle favorites per track |
| Sleep timer | Stop playback after presets, a custom 1-1440 minute duration, or the current track, with optional 10-second, 30-second, 1-minute, or 2-minute fade-out |
| Repeat/shuffle | Persisted shuffle flag and repeat mode |
| Provider architecture | `MusicSourceProvider`, optional paging and search-suggestion extensions, `MusicCatalogProvider`, optional `MusicCatalogPagingProvider`, `MusicCatalogDiscoveryProvider`, optional `MusicCatalogDiscoveryPagingProvider`, and `MusicCatalogRadioProvider`, `MusicPlaylistMutationProvider`, and `LyricsProvider` interfaces with privacy/network disclosure, offline cache/download policy gates, persisted offline request queue, user-triggered checksum-verified private media cache storage, resilient paginated unified music-provider search, LRCLIB plain/synced lyrics search, demo provider, Podcast RSS feeds, Radio Browser, Internet Archive, and configurable Jellyfin/Navidrome/Subsonic accounts with secure credentials, portable HTTPS-only account-configuration export/import that requires credentials to be entered on each device, test-before-replace credential rotation, salted Subsonic tokens, paginated catalog/discovery browsing, provider-backed radio, credential-safe artwork, and remote playlist editing |
| Documentation | README, feature matrix, architecture, user guide, release guide, legal notes |
| GitHub readiness | 0BSD license, CI workflow, issue templates, contribution guide, security policy |
| Proof gates | CI analyzes/tests Flutter, runs provider and system-media contract tests, compiles an Android APK and unsigned iOS app, builds all desktop targets, analyzes/tests/compiles the server, assembles checksummed tag/manual release bundles, and publishes version-tag GitHub Release assets |

## Feature Goal

AetherTune is designed to support the combined feature categories users expect from modern free/open music clients:

| Feature category | Target support |
|---|---|
| Local files | Library import, recursive folder scanner with filename metadata parsing, album artist/release year/track number from ID3v2, FLAC/Ogg/Opus, M4A, and applicable WAV RIFF INFO tags, playback, search, favorites, durable 0-5 ratings, rating sort, imported-folder browsing, stored and embedded metadata editing for those fields, private file/web artwork overrides for local tracks, duplicate resolver scaffold, folder-watch roadmap, and richer tag scanner/writer roadmap |
| History/stats | Recently played, local play counts, typo-tolerant history search, persisted saved range/query views, per-play entry deletion, estimated listening time, responsive top-track/top-artist bar charts, accessible calendar heatmaps, monthly/yearly calendar recap cards with saveable PNG share visuals and persisted Midnight/Daylight/Signal/Monochrome themes, JSON/CSV export, top track/artist/album/genre recap, and a persisted pause-listening-history privacy toggle implemented; more chart dimensions remain roadmap |
| Backup/restore | Versioned JSON library backup with native file save/open and view/paste fallback |
| Streaming providers | Pluggable provider interface with declared capabilities, permissions, network disclosure, and cache/download policy gates for legal source adapters; optional user-keyed official YouTube Data API and user-authorized Spotify Web API sources provide metadata/artwork search only, with bounded official type-ahead, never YouTube or Spotify playback, downloads, or offline media |
| Offline | Local-first data model, offline library filter, persisted offline mode that pauses network-backed source actions and player-wide saved stream playback, per-provider cache/download policy gate, persisted pause/resume-capable cache/download queue manager, checksum-verified private direct-URL cache storage with HTTP Range retry resume, usage/trim/clear/app-provider quota controls, user-chosen folder export, and automatic post-cache pressure eviction; background download jobs remain roadmap |
| Music discovery | Local Home feed sections, user-refreshed self-hosted discovery lists (Jellyfin recently added albums; Navidrome/Subsonic newest, frequently played, recently played, and random albums; provider playlists), a bounded followed-artist shelf derived from matching server recently-added albums, explicit deduplicated continuation for supported Navidrome/Subsonic discovery shelves, range-filtered local charts, local mood/activity mixes, explainable personalized local recommendations with independent favorite/history controls, similar local tracks, related local artist/album pages, seed-based local radio, collection-aware local radio, and self-hosted track/artist/album radio through Jellyfin Instant Mix and Subsonic similar songs implemented; provider-native creator/channel subscriptions, provider editorial/personalized feeds, provider charts, provider moods, official-provider radio, and broader provider-backed artist/album recommendations remain roadmap |
| Lyrics | Plain text, synced LRC, cue-timed SRT, and word-timed TTML karaoke editing, UTF-8 file/sidecar import, user-triggered LRCLIB search with local match ranking and attributed persistence, optional on-demand translation through a user-configured LibreTranslate-compatible endpoint, native TXT/LRC/SRT/TTML save dialogs and copyable export text, local saved-lyrics search, playback-linked line/word highlighting, bounded excerpts, selected-line sharing, and PNG lyric-card save/native sharing implemented; more provider adapters and rights-aware visual sharing remain roadmap |
| Playlists | Manual playlists, private local-image or web artwork, built-in smart playlists, persisted custom smart rules with Match all/Match any selection and bounded recursive rule groups, in-playlist search, track reordering, native JSON/M3U/CSV file import/export with paste/view fallback, copyable share text, save-queue-as-playlist, native share-sheet PNG cards, and manual whole-library snapshot sync implemented; automatic per-playlist merge, synced rules, custom smart-playlist artwork cropping/collages, and artwork-file sync remain roadmap |
| Android integrations | Queue-aware media notification, artwork/metadata, play/pause/seek/previous/next/stop, repeat/shuffle, media-button receiver, foreground media service, background playback configuration, device-adaptive native equalizer presets/custom bands, native loudness enhancement, and Android Auto current-queue, library, nested local-folder, artist, album, genre, source, manual-playlist, built-in-smart-playlist, and custom-smart-playlist browsing implemented. Car-library selections preserve collection order and duplicate entries, then flow through the same resolver, offline policy, queue persistence, and output settings as an in-app play action. The generated home-screen widget provides previous, play/pause, and next through that same media-button receiver, mirrors the current title, artist, and play/pause state, and the launcher exposes static Previous, Play/Pause, and Next shortcuts through the same route; Android 8+ Options can also request a user-pinned Previous, Play/Pause, or Next shortcut when supported by the launcher. Physical-device audio-effect/lifecycle tests, widget artwork, Android Auto emulator/vehicle validation, and distraction-optimized car UX review remain roadmap |
| iOS integrations | Control Center and lock-screen queue metadata/artwork/transport controls plus music-session background audio configuration implemented; physical-device interruption/lifecycle tests and CarPlay browsing remain roadmap |
| Desktop | Linux/macOS/Windows build support with a responsive navigation rail, a persisted Comfortable/Compact density choice, and a resizable queue pane at desktop widths; global hotkeys, tray/menu bar, and installer polish roadmap |
| Server | Health/info/catalog APIs, versioned authenticated library snapshots, durable latest-revision storage, nested static multi-device credentials, a durable operations-managed account/device-token lifecycle with rate-limited last-authentication metadata, and authenticated self-service managed account/device naming are implemented; optional client-side automatic foreground upload is available, while public registration/password/OAuth login, automatic background sync, deployment hardening, and remote provider coordination remain roadmap |
| UI customization | Material 3 shell with persisted system/light/dark/AMOLED theme preference and persisted System colors or named accent swatches. System colors uses supported platform dynamic color schemes while named swatches remain explicit overrides; AMOLED keeps its black surfaces |
| Localization | Generated Flutter localization delegates with English, Turkish, and Arabic catalogs for the app shell, responsive navigation, and onboarding; Options persists a System/English/Turkish/Arabic override while the remaining feature UI is translated incrementally |
| Privacy | No telemetry, no ads, no tracking, no forced account, provider disclosure, a local pause-listening-history control, and optional user-authorized ListenBrainz completed-listen submissions with its token held only in the platform credential vault; offline mode suppresses submissions |
| Multi-source | Local provider support plus offline-mode network pausing and saved stream playback blocking, unified provider search across the local library and legal adapters with explicit opaque-cursor continuation, cross-page reranking/deduplication, retained-results partial-failure retry, and offline local-only paging, user-configured domain-restricted JSON catalogs with paged search/type-ahead and direct playback, optional securely stored user-keyed YouTube Data API metadata/thumbnail search and user-authorized Spotify PKCE metadata/artwork search (both deliberately audio-free), public Apple podcast-directory discovery that hands valid RSS feeds to the existing subscription flow, Podcast RSS feed subscriptions/play/save/OPML/refresh status/progress resume/cache-download queue and private cache eligibility/eviction, Radio Browser mirror discovery/search/filter/stream validation/play/save/click accounting with live-stream cache/download denial, paginated Internet Archive audio search/filter/facet suggestions/item details/play/save/cache-download queue and private cache eligibility/eviction with multi-file item results, and Sources-tab Jellyfin plus Navidrome/Subsonic accounts with connection testing, secure secret storage, atomic credential rotation with rollback, salted Subsonic tokens, search, runtime-only stream resolution, retained-results paginated catalog browsing, self-hosted track/artist/album radio, bounded credential-safe in-app/system artwork, and remote playlist create/rename/delete/add/remove/reorder; portable manual library snapshots can cross devices without provider credentials or private media, while automatic merge and broader official-provider features remain roadmap |

For the full truth table, see [`docs/FEATURE_MATRIX.md`](docs/FEATURE_MATRIX.md). The matrix separates **implemented**, **scaffolded**, **planned**, and **not included** features so the project does not make fake “100% done” claims.

## Project Layout

```text
aethertune/
├─ apps/mobile/                 # Flutter client app for mobile and desktop
│  ├─ lib/src/domain/            # Track and provider contracts
│  ├─ lib/src/data/              # Local persistence and demo provider
│  ├─ lib/src/player/            # Playback controller
│  └─ lib/src/ui/                # App UI
├─ services/server/              # Dart HTTP server package
├─ docs/                         # Architecture, matrix, user/release/legal docs
├─ scripts/                      # Bootstrap and check scripts
├─ .github/workflows/            # GitHub Actions CI
├─ LICENSE                       # 0BSD license
└─ README.md                     # Landing page
```

## Why this name?

**AetherTune** = music that can come from many “air”/network/local sources while remaining transparent, open, and user-controlled.

## Non-goals

AetherTune is not a piracy tool, not a YouTube Music Premium replacement, not a DRM bypass project, and not a private API scraper. The right way to expand it is through legal, documented, or user-owned sources.

## Contributing

Pull requests are welcome. Start with [`CONTRIBUTING.md`](CONTRIBUTING.md), then check [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) and [`docs/API_PROVIDERS.md`](docs/API_PROVIDERS.md).

## License

AetherTune is released under the **BSD Zero Clause License (0BSD)**. See [`LICENSE`](LICENSE).
