name: aethertune_server
description: HTTP server for AetherTune health checks, catalog metadata, and future sync APIs.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  shelf: ^1.4.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8
