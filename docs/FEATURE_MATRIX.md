# Feature Matrix

This matrix is intentionally honest. AetherTune can be 100% free/open-source, but it is not yet 100% feature-complete against every app named below. This document defines the complete parity target and separates what is already real from what still needs implementation.

Reference apps named by the user:

Kreate, OpenTune, InnerTune, SimpMusic, ArchiveTune, Spotube, Echo Music, Namida, PipePipe, NewPipe, LibreTube, Musify, AuraMusic, Bloomee Tunes, Gyawun Music, Music You, Muzza, SoundPod, NouTube, Grayjay, OuterTune, ViTune, RiMusic, Harmony Music, YMusic, YouTube Music, Flow, and MetroList.

## Status Legend

- **Done**: implemented in code, documented, and covered by a relevant check where practical.
- **Scaffolded**: architecture or placeholder exists, but the user-facing feature is not complete.
- **Roadmap**: planned here, but no meaningful implementation exists yet.
- **Blocked / official-only**: possible only through user-owned data, open catalogs, or official/legal APIs.
- **Not included**: intentionally excluded for privacy, legal, platform-policy, or safety reasons.

## What "100% Coverage" Means

For this project, "100% coverage" means the matrix covers the full feature surface needed to compete with the named apps. It does not mean every feature is already implemented.

To claim 100% implemented parity later, AetherTune must satisfy all of these gates:

| Gate | Required evidence |
|---|---|
| Feature exists | User-facing code path, not just documentation. |
| Cross-platform behavior | Verified on every supported target that claims the feature. |
| Provider legality | Uses local files, user-owned servers, open catalogs, public feeds, or official APIs. |
| Tests/checks | Unit, widget, integration, build, or workflow evidence matching the feature risk. |
| Documentation | User guide, release notes, and support notes updated. |
| Failure handling | Clear empty, loading, error, offline, and permission states. |
| Accessibility | Keyboard, screen reader, contrast, reduced motion, and touch target checks where applicable. |
| Privacy | No hidden telemetry, credential leakage, private API scraping, or DRM bypass. |

## Implemented Foundation

| Feature | Status | Current evidence | Needed for full parity |
|---|---:|---|---|
| Android/iOS app shell | Done | Flutter app with Material 3 UI. | Store signing, background modes, platform polish. |
| Linux/macOS/Windows desktop build | Done | CI builds debug desktop targets; wide layouts use a navigation rail, keyboard navigation, a functional persisted-resizable queue split pane, native tray close behavior, and a device-local Comfortable/Compact density control. | Installers and update channel. |
| Server package | Done | Dart HTTP service under `services/server` with static raw or SHA-256-digest bearer-token configuration, durable versioned sync snapshots and revision-preserving reset tombstones, loopback-only-by-default Docker/Compose deployment with health checks, aggregate no-tracking process metrics, privacy-preserving structured request logs, and checked-in Caddy/systemd deployment artifacts. | Provider coordination, account lifecycle, and production TLS/reverse-proxy automation. |
| Server health/info/catalog/metrics endpoints | Done | Covered by server tests and CI compile gate; Docker health checks call `/health`, while `/api/v1/metrics` exposes aggregate uptime/request-count/sync-state data only. | Real persisted catalog and provider-coordination APIs. |
| MIT license | Done | Root `LICENSE`. | Third-party notice automation. |
| No telemetry | Done | No analytics SDK or tracking dependency. | Privacy tests and network-call audit. |
| Local audio import | Done | Native file picker plus recursive folder scanner for supported audio extensions, including filename track-number parsing, artist/title parsing, basic ID3v1 MP3 title/artist/album tag parsing, basic ID3v2 MP3 title/artist/album/genre text-frame and APIC/PIC artwork parsing, basic FLAC Vorbis comment title/artist/album/genre and picture artwork parsing, basic Ogg Vorbis and Opus comment title/artist/album/genre plus standard picture artwork parsing, basic M4A title/artist/album/genre atom plus `covr` artwork parsing, basic WAV RIFF INFO title/artist/album/genre parsing, and matching `.lrc`/`.txt` lyric sidecar import with scanner tests. Track rows and the player bar render local/provider artwork with a fallback icon. | Folder watch, scoped storage UX, richer embedded tag parsing, tag writing, and artwork editing. |
| Local playback | Done | `just_audio` local file playback uses one lazy native playlist; Android/iOS/macOS use native plugin backends, Linux/Windows bundle initialized MediaKit audio backends, and Android/iOS/macOS wrap playback in a queue-aware system media session. | Physical-device codec and lifecycle matrix. |
| Stream URL playback | Done | Player accepts legal direct stream URLs. | Provider resolver UI, retries, caching, auth headers. |
| Library persistence | Done | `shared_preferences` JSON store. | SQLite/Drift schema and migrations. |
| Backup/restore | Done | Versioned JSON backup/restore with native file save/open dialogs and JSON view/paste fallback, including theme/accent preferences, pause-listening-history and recommendation-signal preferences, offline mode, and queued offline media requests. Store and backup-document tests cover the portable format. | Migration tooling and cloud targets. |
| Stored metadata editing | Scaffolded | Track menus edit saved title, artist, album, and genre, and local tracks can replace artwork with a validated private PNG/JPEG/GIF/WebP file or an http/https URL, then restore scanned artwork. Local M4A tracks can also transactionally replace their embedded cover with a PNG/JPEG under 512 KiB; standard front-loaded M4A files repair validated `stco`/`co64` offsets, and the UI immediately refreshes the scanned cover while preserving any existing private/web override. Manual file artwork stays on the current device; portable backup/sync/playlist documents retain safe web or embedded artwork and never expose private file paths. Store/file-store and M4A writer tests cover persistence, scanner refresh, preservation, offset repair, and failure safety. | Artwork crop/editing, scanner reconciliation after unrelated external edits, fragmented/unsupported M4A layouts, and rollback handling. |
| Duplicate resolver | Scaffolded | Options tab finds duplicate library tracks by normalized local path, scanner content hash, provider/external ID, stream URL, or metadata plus known duration. Reviewers can select non-overlapping groups, choose a keeper per group, and merge the batch atomically while preserving playlists, favorites, lyrics, history, progress, and per-track playback speed. A revision-protected Undo last merge action restores the full prior state for an individual or batch operation; scanner/store tests cover content hashes, detection, persistence, batches, overlap protection, and state restoration. | Audio fingerprinting and scanner reconciliation. |
| Search | Done | Typo-tolerant local title/artist/album/genre/source/folder/saved-lyrics filtering plus submitted-query/playback/metadata suggestion chips and Sources-tab provider fan-out/ranking with local-library merge and configured Jellyfin/Navidrome/Subsonic accounts. Unified search uses optional opaque cursors across Local Library, Demo, Radio Browser, Internet Archive, Jellyfin, and Subsonic; **Load more provider results** appends, deduplicates, and re-ranks pages, while partial failures retain prior rows and retry only failed cursors. New searches invalidate stale replies, non-progressing cursors stop, and offline searches page local results without network providers. Dedicated Internet Archive and Radio Browser catalogue surfaces retain their filters and pagination. Coordinator, exact adapter-query, and Sources widget tests prove continuation, totals/exhaustion, ranking/deduplication, retained failures, and same-cursor retry. | Richer global relevance, provider-specific facets in unified results, and paging adapters for future providers. |
| Recently added / library sort | Done | Store sort modes, Library sort menu, and unit coverage. | More smart filters and saved views. |
| Favorites | Done | Toggle and filter favorites; same-track manual sync merges favorite state from either device so a marked favorite is not discarded by local metadata precedence. | Richer smart filters and import/export. |
| Queue | Done | Current list is loaded once as a native playlist, follows automatic engine index transitions, restores across app launches, preserves current position while reordered/trimmed, filters stream entries in offline mode, and can be saved as a playlist. Credential-bearing self-hosted URLs are runtime-only, stripped from snapshots, reconstructed from secure storage after restart, removed with their provider account, and stripped/re-resolved after credential rotation. Rotation stops the loaded queue before replacement, preserves position/play state on success, and leaves unresolved current tracks metadata-only and stopped. Controller tests cover those boundaries plus native transitions, mutation rebuilds, persistence, and stop-at-end isolation. | Cross-device queue sync. |
| Recently played / listening history | Done | Persisted playback history with History tab, typo-tolerant search over recently played tracks and per-play entries, persisted named range/query views with create/apply/update/rename/delete UI and backup coverage, per-play entry deletion, play counts, estimated listening time, date-range filters, monthly/yearly calendar recap cards, saveable PNG recap visuals with persisted Midnight/Daylight/Signal/Monochrome palettes, JSON/CSV stats export, top track/artist/album/genre recap, clear action, and a persisted pause-listening-history toggle that stops new play/progress writes while preserving existing history until cleared. | Cross-device view sync. |
| Manual playlists | Done | Persisted user playlists support add/remove/find/reorder/import/export/play, folders with All/Unfiled filtering and move-to-folder actions, explicit private local-image or web artwork, and generated collages from up to four ordered playlist tracks in the list and playlist sheet. Native JSON/M3U/CSV file save/open and view/paste fallbacks are implemented. Store and widget tests cover artwork behavior, folders, portable JSON, and backup restore. | Artwork cropper and richer collaboration. |
| Smart playlists | Scaffolded | Playlists tab exposes built-in dynamic Favorites, Recently added, Recently played, and Most played collections plus persisted custom smart playlists with a Match all/Match any control over search text, exact source ID, artist, album, genre, optional minimum/maximum duration in seconds, favorites-only, minimum play count, and minimum days since last play (including never-played tracks), plus sort and result limits. Additional rule groups can themselves contain Match all/Match any rules and nested groups, enabling expressions such as `artist = Mira AND (genre = Rock OR genre = Jazz)`; malformed imported groups are ignored safely. The recursive editor and persistence layer share enforced bounds of 25 groups per level, 50 rules per group, and eight group levels. Custom rules have explicit private local or web artwork, with matching-track artwork as a fallback; private artwork is stripped from backup/sync documents. Bounded AetherTune import links round-trip dynamic queries and nested groups while excluding private artwork; supported `aethertune://` links import once and focus Playlists across Android, iOS, Linux, macOS, and Windows. Store and deep-link listener tests cover matching modes, nested evaluation, artwork privacy, portable links, persistence, update/delete, duplicate suppression, and backup restore; a 390x844 HomeScreen widget test proves recursive creation, evaluation, persistence, lifecycle safety, phone layout, and all editor bounds. | Sync and server/public dynamic-query sharing. |
| Plain/LRC lyrics | Done | Persisted per-track lyrics editor with LRC timestamp parsing, preview, playback-linked highlighting/autoscroll, UTF-8 file import, matching folder-scan sidecar import, user-triggered LRCLIB plain/synced search, attributed local caching, native TXT/LRC save dialogs, and copyable export text with suggested filenames. | More provider adapters and richer sharing. |
| Next/previous | Done | Queue navigation. | Media key and lock-screen integration. |
| Sleep timer | Done | 5/15/30/60/90 minute presets, custom 1-1440 minute duration, end-of-current-track mode, optional 10-second/30-second/1-minute/2-minute fade-out, and unit coverage for timer/fade rules. | Platform media-session integration. |
| Shuffle | Done | `just_audio` shuffle flag is persisted across app launches. | Queue-aware shuffle polish. |
| Repeat one/all/off | Done | `just_audio` loop mode is persisted across app launches. | UI tests and platform media-session integration. |
| Provider plugin contract | Done | `MusicSourceProvider` requires capability flags, privacy/network disclosure, and cache/download policy inputs. | Stable provider SDK, packaging, sandbox rules. |
| Demo provider | Done | Metadata-only provider template. | Real providers listed below. |
| Podcast RSS subscriptions | Scaffolded | Sources tab adds/removes persisted RSS feed subscriptions, imports/exports OPML, retains up to 100 playable episodes per feed for offline browsing and Home, loads or refreshes one feed or all saved feeds sequentially with per-feed status/failure recording, tracks refresh status/staleness, plays/saves episodes, queues cache/download requests after provider policy approval, materializes direct enclosure URLs into checksum-verified private cache storage with HTTP Range retry resume, usage/app-provider quota eviction controls, resumes saved episode progress, includes backups and sync merge, declares cache/download policy for legal enclosures, and provider/cache/store behavior has tests. | Background download jobs. |
| Radio Browser station search | Scaffolded | Sources tab searches Radio Browser, discovers a public API mirror before default searches with fallback to the bundled mirror, filters by country/language/tag/codec/bitrate, paginates results with retained-results retry and duplicate protection, opens station details with directory status/tags/codec/homepage/stream metadata, validates selected station stream reachability/content type, plays public streams, sends station click accounting on playback, saves stations to the library, denies cache/download policy for live streams, and provider parsing/filter/mirror/pagination/validation/click/detail behavior has tests. | Deeper codec probing and retry/backoff policy. |
| Internet Archive audio search | Scaffolded | Sources tab searches public Internet Archive audio, filters by collection/subject/creator/year, exposes returned collection/subject/creator/year facet chips, paginates results with an explicit continuation button, exhaustion state, and retry-safe retained results, opens each result into a detail page with collection/subject/license metadata and every playable file, resolves file URLs, plays/saves tracks, queues cache/download requests after provider policy approval, materializes direct public file URLs into checksum-verified private cache storage with HTTP Range retry resume, usage/app-provider quota eviction controls, declares cache/download policy for public files, and provider/cache/search/filter/detail behavior has tests. | Background download jobs. |
| Offline mode | Done | Options tab has a persisted offline mode that pauses network-backed provider search, feed refresh, Sources playback actions, and player-wide saved URL stream playback; Library has a local-files-only offline-ready filter; backup/restore, source classification, offline playback policy, direct URL cache materialization/manual eviction, checksum metadata, HTTP Range retry resume for direct URL cache writes, automatic cache pressure eviction, app/provider cache limits, and library filter tests cover behavior. | Background downloader jobs. |
| CI proof gates | Done | Flutter analyze/test, Android and unsigned iOS compilation, desktop builds, server analyze/test/compile, generated secure-storage entitlement/config tests, deterministic resolved dependency inventories and CycloneDX 1.5 SBOMs, and tag/manual release artifact workflow. | Physical-device integration tests. |

## Full Parity Feature Surface

### Playback And Audio Engine

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Background audio | Scaffolded | YouTube Music, InnerTune, RiMusic, NewPipe, YMusic | Android/iOS/macOS initialize a music `AudioSession` and `audio_service` handler around the same native queue engine; generated Android/iOS wrappers receive validated foreground-service/background-audio settings, and CI compiles Android plus unsigned iOS apps. Needed next: physical-device interruption, route, battery, and process-lifecycle fixtures. |
| Notification controls | Scaffolded | YouTube Music, Namida, MetroList | Android declares the foreground media service and media-button receiver; the handler publishes queue/current item metadata, artwork, duration, position, buffering state, repeat/shuffle state, and previous/play-pause/next/stop actions. Unit tests prove state publication and command routing; physical notification/device tests remain. |
| Lock-screen controls | Scaffolded | YouTube Music, Namida | Android/iOS/macOS receive the current queue item, artwork, playback state, position, seek, repeat, shuffle, and transport callbacks through `audio_service`. Needed next: physical Android/iOS lock-screen and Control Center fixtures. |
| Media keys | Scaffolded | Desktop players, Namida | macOS receives `audio_service` system transport callbacks; Android/iOS headset/media buttons use the same handler. Windows, Linux, and macOS also register independent global play/pause, previous, and next media keys through the desktop hotkey manager, tolerate an OS-reserved key without losing the others, unregister on shutdown, and retain focused-window fallbacks. Controller tests cover registration, routing, partial failure, and unsupported platforms. Needed next: physical-host media-key fixtures and native Windows SMTC/Linux MPRIS publication. |
| Gapless playback | Scaffolded | Namida, YouTube Music | Queue playback now uses a lazy `ConcatenatingAudioSource` instead of per-track reloads; automatic native index transitions update app state without replacing the source, repeat stays in the engine, controller tests prove the no-reload contract, and MediaKit playlist prefetch is enabled for bundled Linux/Windows backends. Needed next: acoustic transition fixtures on physical Android/iOS/macOS devices and Windows/Linux audio hosts before a universal zero-gap claim. |
| Crossfade | Scaffolded | Namida, YouTube Music | The Just Audio engine preserves normal lazy gapless queues when disabled and, when enabled, uses a second player to overlap/fade the next track into the current native queue position. It applies the controller's per-track ReplayGain envelope to each side of the overlap and persists 1/2/3/5/8-second choices. Crossfade declines shuffled queues, repeat-one, and tracks without known duration rather than guessing a next item. Needed next: acoustic/device fixtures and backend-native gapless crossfade where available. |
| ReplayGain / loudness normalization | Scaffolded | Namida, local music players | Local FLAC/Ogg/Opus comments, MP3 ID3v2 `TXXX`, and M4A `----` freeform tags read bounded native `REPLAYGAIN_TRACK_GAIN` and `REPLAYGAIN_ALBUM_GAIN` metadata. Options can persistently select track or album gain, with a safe fallback to the other valid tag, and apply the selected gain as a bounded output multiplier over the user volume during playback, queue transitions, and sleep fades. Needed next: EBU R128, peak limiting, and loudness analysis. |
| Equalizer | Scaffolded | Namida, Musify, RiMusic | Android production playback attaches a native `AndroidEqualizer` to both the main and crossfade `just_audio` pipelines. Options persists enablement plus frequency-based Flat/Bass boost/Vocal/Treble profiles, discovers each device's actual band centers/ranges after source activation, and persists custom band gains. The system-media wrapper forwards the optional capability, unsupported platforms hide it, and model/controller/wrapper/widget tests cover the contract. Needed next: physical-device acoustic fixtures plus iOS and desktop DSP backends. |
| Bass boost / virtualizer | Scaffolded | Android music clients | Bass boost is a real low-frequency curve applied through the Android session equalizer and mirrored into crossfade. A separate 0-12 dB native Android loudness enhancer is persisted and capability-gated. Needed next: Android virtualizer support, clipping/acoustic fixtures, and non-Android equivalents. |
| Loudness enhancement | Scaffolded | Android music clients | Options drives `AndroidLoudnessEnhancer` target gain independently from ReplayGain normalization and applies the same enabled/gain state to the primary and crossfade sessions. Needed next: physical-device output fixtures and non-Android enhancement backends. |
| Pitch / tempo control | Scaffolded | Podcast/video clients | Android and the bundled Linux/Windows MediaKit backends expose independent 0.5x, 0.75x, 1x, 1.25x, 1.5x, and 2x pitch factors through the shared playback engine. Options and Now Playing set a persisted default without changing the existing playback-speed or per-track speed-override behavior; system-media wrappers forward the setting on supported Android sessions. Unsupported hosts hide both controls. Controller, system-wrapper, desktop Options, and Now Playing tests cover persistence, forwarding, capability boundaries, and selection. | iOS/macOS pitch backends, per-track pitch overrides, and pitch-preserving audio-analysis fixtures. |
| Configurable skip intervals | Done | NewPipe, PipePipe, podcast/music clients | Options persists independent 5/10/15/30/45/60-second backward and forward intervals. Full Now Playing exposes dedicated rewind/forward controls that clamp seeks to the current track bounds. |
| Skip silence | Scaffolded | Podcast/music clients | Android Options exposes a persisted **Skip silence** toggle only when the active backend supports it. The shared playback engine applies the preference to both the normal and crossfade `just_audio` players, so quiet passages remain consistently shortened through track transitions. System-media forwarding and controller persistence tests cover the capability boundary. Other backends remain unsupported. |
| Audio-only mode for video sources | Blocked / official-only | YouTube Music, YMusic | Only through official APIs or user-provided legal streams. |
| Playback speed | Done | NewPipe, PipePipe, LibreTube, podcast and music players | The shared audio engine supports 0.5x, 0.75x, 1x, 1.25x, 1.5x, 2x, 2.5x, and 3x playback. Options sets a persisted default and Now Playing can apply or clear a device-local speed override for the current track without changing that default; automatic track changes restore the applicable override or default. Supported system media sessions receive the effective speed. Pitch-preserving DSP remains future work. |
| Playback volume | Done | Music players | Now Playing and Options expose a 0-100% output-volume slider. It previews while dragging, persists on release, restores before playback, and disables only while an active sleep fade owns output volume. |
| Sleep timer with fade/custom rules | Done | InnerTune, Namida, YouTube Music | Presets, custom 1-1440 minute duration, end-of-current-track action, opt-in 10-second/30-second/1-minute/2-minute fade-out, volume restore on cancel/stop, and unit-tested fade timing/volume rules are implemented. Needed next: media-session-aware sleep timer notifications. |
| Output device picker | Roadmap | YouTube Music, desktop players | Platform route picker and Bluetooth/cast support. |

### Library And Local Files

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Local file import | Done | Namida, Musify | File picker import and recursive folder scanning are implemented, including common filename metadata parsing, basic ID3v1 MP3 tag parsing, basic ID3v2 MP3 text-frame and APIC/PIC artwork parsing, basic FLAC Vorbis comment and picture artwork parsing, basic Ogg Vorbis/Opus comments and standard picture artwork, basic M4A metadata atom plus `covr` artwork parsing, and basic WAV RIFF INFO tag parsing. Folder imports persist a watched root that resyncs relevant filesystem changes while the app runs and when it opens; richer tag formats and scoped-storage polish remain. |
| Folder browsing | Scaffolded | Namida, local music players | Imported-track flat folder groups, recursive folder import, recursive folder tree browsing with aggregate parent counts, descendant playback queues, privacy-safe folder share text, and persisted watched roots are implemented. Where the platform filesystem supports monitoring, watched folders debounce relevant audio/lyrics changes, rescan safely on startup and refresh, preserve favorites/added timestamps for stable local IDs, and avoid deletion after incomplete scans. Platform storage permission polish remains. |
| Metadata scanner | Scaffolded | Namida | Folder import derives album/folder metadata from parent folders, parses common filename patterns such as leading track numbers and `Artist - Title`, reads basic ID3v1 MP3 title/artist/album tags, reads basic ID3v2 MP3 title/artist/album/genre text frames, APIC/PIC artwork, bounded `TXXX` ReplayGain tags, and bounded `USLT`/legacy `ULT` lyrics, reads FLAC/Ogg/Opus Vorbis metadata, artwork, ReplayGain, and `LYRICS`/`UNSYNCEDLYRICS` comments, reads M4A title/artist/album/genre plus `covr`, bounded `----` ReplayGain, and standard `©lyr` lyrics, and reads basic WAV RIFF INFO title/artist/album/genre tags before falling back to filenames. Watched folders reconcile changed content hashes and new/deleted files while retaining user state; saved lyrics win, sidecars win over embedded tags, and neither overwrites an existing lyric. Scanner/watcher tests cover recursive import, relevant-event filtering, debounce, sidecars, cross-format embedded-lyric precedence, incomplete-scan deletion protection, dashed titles, ID3v1/ID3v2/FLAC/Ogg/Opus/M4A/WAV tag preference, UTF-16 ID3v2 text, partial tag merges, artwork extraction, ReplayGain extraction, and tag fallback. Needed next: background indexing and richer tag formats. |
| Metadata editing | Scaffolded | Namida | Track menus edit persisted library title, artist, album, and genre, and local tracks can choose a validated private PNG/JPEG/GIF/WebP cover or http/https cover URL, then restore their scanned embedded artwork. Private covers are retained only on that device; backup, sync, and playlist documents fall back to safe embedded artwork or retain a safe web URL. Search, browse groups, suggestions, playlists, and backup data update from the edited record. Confirmed local MP3 edits write title, artist, album, and genre into ID3v2.3/v2.4 text frames, retain an ID3v1 compatibility tag, preserve supported non-editable v2 frames such as artwork, preserve audio bytes through a transactional copy, and refuse unsupported tag layouts before modifying the file. Confirmed local FLAC edits update standard Vorbis comments, preserve non-comment metadata blocks such as artwork plus custom comments and audio bytes through the same transactional copy pattern, and reject malformed comments before mutation. Confirmed local Ogg Vorbis and Opus edits update standard isolated comment-header pages, preserve custom comments and all media pages through a transactional copy, recalculate the edited page CRC, and refuse shared, continued, malformed, or nonstandard comment-page layouts. Confirmed local WAV edits update RIFF INFO fields, preserve unrelated RIFF chunks, custom INFO fields, and data bytes transactionally, and reject malformed INFO lists before mutation; legacy INFO text falls back to `?` outside Latin-1. Confirmed local M4A edits update standard title/artist/album/genre `ilst` atoms with UTF-8 text and can replace the `covr` cover with a PNG/JPEG under 512 KiB. The transactional copy preserves unknown metadata items and media bytes, repairs `stco`/`co64` offsets when a standard front-loaded `moov` changes size, refreshes the scanned cover in the library, and refuses malformed, oversized, extended-size, or fragmented layouts before mutation. Focused tests prove scanner round-trips, replacement/preservation, Ogg CRC generation, front-loaded offset repair, portable artwork privacy, and failure safety. Needed next: artwork crop/editing, scanner reconciliation after unrelated external edits, support for fragmented/other nonstandard M4A layouts, and rollback recovery after an external filesystem failure. |
| Duplicate resolver | Scaffolded | Local library apps | Options duplicate resolver detects path, scanner content hash, provider item, stream URL, and metadata plus known-duration matches. Its batch review selects only non-overlapping groups, chooses a keeper per group, and merges them atomically with playlists/history/lyrics/progress and per-track speed preserved. A revision-protected Undo last merge action restores the complete pre-merge individual or batch state. Needed next: audio fingerprinting and scanner reconciliation. |
| Album/artist/genre/source/folder views | Done | YouTube Music, Namida | Library browse groups tracks by artist, album, genre, source, and imported folder. Artist and album groups open responsive full-screen pages with representative artwork, live track/favorite/play/duration stats, ordered play, shuffle, save-as-playlist, unchanged track actions, artist follow and album-to-artist navigation, visual/text sharing, artist album shelves, and explainable related local artists/albums. Genre/source groups retain compact sheets and folders retain the recursive tree. Store/widget tests cover grouping, related ranking reasons, collection saving, page navigation, playback, shuffle, and mobile layout. The scanner reads basic ID3v2 MP3, FLAC Vorbis, M4A, and WAV RIFF INFO genre text plus embedded MP3/FLAC/M4A artwork. Explicit provider album IDs, album artist/year/track-number metadata, and richer tag formats remain. |
| Recently added / recently played | Done | YouTube Music, Namida | Recently added sorting/API and recently played history are done; recently played lists can be searched by track metadata and saved lyrics, and named history range/query views persist across restarts and backups. Saved recently-added filter presets remain. |
| Listening history | Done | YouTube Music, Last.fm-style clients | Playback history, range exports, clear history, per-play entry deletion, typo-tolerant history search, and pause-listening-history privacy control are implemented. |
| Stats / recap | Scaffolded | YouTube Music, Namida | Store-level stats aggregate local tracks, favorites, play counts, estimated listening duration, and top tracks/artists/albums/genres; Home and History show responsive normalized bar charts for top tracks/artists; History filters stats by all time, last 7 days, last 30 days, or last year, persists named range/query views, shows an accessible scrollable day-by-day listening heatmap plus monthly/yearly calendar recap cards, and exports each recap as a fixed-size PNG through the native save dialog. Recap previews offer labeled color swatches for Midnight, Daylight, Signal, and Monochrome; the selected palette is rendered into the PNG and persists through local settings, backups, and manual sync snapshots. Store/card tests cover preference fallback and round trips, all four renderers, swatch selection, and PNG capture; a 390x844 HomeScreen test proves the live History workflow and layout. JSON/CSV export, per-play deletion, history search, and pause-listening-history remain implemented. Needed next: more chart dimensions. |
| Backup/restore | Done | Namida, local-first apps | Includes local library data, app theme/accent and listening-recap visual theme preferences, pause-listening-history and recommendation-signal preferences, offline mode, and queued offline media requests; native file save/open dialogs use a portable UTF-8 JSON document while view/paste remains available. Cloud targets and migration checks remain. |
| Cross-device library sync | Done | YouTube Music, Grayjay-style multi-device needs | An optional self-hosted server stores an authenticated, checksum-verified versioned library snapshot. The app keeps the bearer token in secure storage, tests configuration before saving, uploads/downloads explicitly, can confirm a remote-only snapshot reset that creates a conflict-preserving tombstone and disables automatic uploads, preserves device-local paths and cache settings on restore, and requires a choice on revision conflict. A manual Merge both option unions independent records, combines playlist and podcast catalogs, retains a favorite marked on either device, and pushes the result. Opt-in foreground uploads run at most every 15 minutes and never download, overwrite, or merge automatically. Background scheduling, queue sync, and cross-device provider credentials remain out of scope. |

### Search, Discovery, And Recommendations

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Local library search | Done | All music apps | Searches title, artist, album, genre, source, folder, and locally saved plain/LRC lyric text with typo-tolerant term matching; advanced filters and richer global ranking remain. |
| Multi-provider search | Done | Spotube, Grayjay, Echo Music | `ProviderSearchCoordinator` fans out to the Local Library, Demo Provider, Radio Browser, Internet Archive, and configured Jellyfin/Navidrome/Subsonic accounts, ranks mixed metadata with typo-tolerant scoring, and resolves selected metadata-only tracks. Optional `MusicSourceSearchPagingProvider` adapters keep continuation opaque while mapping local/demo offsets, Radio Browser `offset`/`limit`, Internet Archive `page`/`rows`, Jellyfin `StartIndex`/`Limit` plus totals, and Subsonic `songOffset`/`songCount`. Sources explicitly loads more, deduplicates and re-ranks retained rows, advances successful providers during partial failure, retries only failed original cursors, rejects non-progressing cursors, invalidates stale searches after query/account changes, and pages only the local provider offline. Exact adapter, coordinator, and widget tests cover fallback bounds, totals/exhaustion, retained failures, retry, stale-response protection, and offline zero-request behavior. Future work is richer provider-specific ranking/facets and new provider adapters, not the unified search contract. |
| Search suggestions | Scaffolded | YouTube Music | Library tab suggestion chips come from persisted submitted query history, recent playback, title, artist, album, genre, source, and folder metadata, including typo-tolerant local matching; store tests cover history dedupe, persistence, backup restore, and matching. Needed next: provider suggestions and remote suggestions. |
| Home feed | Scaffolded | YouTube Music, InnerTune, RiMusic | Local Home builds recommendations, mood/activity mixes, continue-listening, recently played, followed-artist additions, radio seeds, most played, favorites, subscribed podcast episodes, recently added tracks, and local charts. A user-triggered **From your servers** refresh queries only configured accounts for bounded, deduplicated shelves: Jellyfin latest music albums; Navidrome/Subsonic newest, frequently played, recently played, and random albums; and provider playlists. The optional discovery-paging contract lets Navidrome/Subsonic append a server-defined shelf with explicit **Load more**, preserves visible albums after duplicate filtering, and stops on an exhausted or non-progressing offset; unsupported catalogs, including Jellyfin latest, retain a bounded shelf. Each discovery or catalog shelf fails independently without exposing raw errors, results open the existing secure collection detail flow, Home makes no implicit request, refresh is disabled offline, and credential-bearing in-memory results clear when the account revision changes. Adapter, coordinator, and mobile/desktop widget tests cover documented request parameters, bounds, dedupe, partial failure, explicit refresh, continuation, navigation, invalidation, layout, and offline zero-request behavior. These server-ordered shelves are not presented as personalized recommendations. Needed next: provider editorial/personalized recommendation feeds and account/provider personalization where legal. |
| Charts / trending | Scaffolded | YouTube Music, OpenTune-style clients | Local charts rank top tracks, artists, albums, and genres for all time, last 7 days, last 30 days, or last year from playback history; responsive normalized top-track/top-artist bar charts are visible on Home and History with compact-layout widget coverage. Needed next: provider-backed chart adapters, trending feeds, region/language filters, and public chart refresh policies. |
| Mood/activity mixes | Scaffolded | YouTube Music | Local Focus, Energy, Chill, Workout, and Sleep mixes are generated from playable library metadata, favorites, play counts, and recency and shown on Home when matching tracks exist. Each preview opens a live full generated-track sheet with play-all, normal track actions, and save-as-editable-playlist behavior. Needed next: provider-curated moods, richer audio-feature metadata, and regional editorial collections. |
| Artist radio / track radio | Scaffolded | YouTube Music, InnerTune | Local seed track radio queues match playable library tracks by artist, genre, or album, then rank them with favorites and play history from every track menu. Home suggests radio seeds with local matches. Full local artist and album pages generate explainable collection-aware queues that can be played or saved. Self-hosted track menus and artist/album pages now use the optional `MusicCatalogRadioProvider`: Jellyfin maps track/artist/album seeds to its playable Instant Mix endpoints; Subsonic maps track/album seeds to `getSimilarSongs.view` and artist seeds to `getSimilarSongs2.view`. Track radio preserves the selected track first, deduplicates server results, resolves streams only at playback, retains the catalog on failure, and issues no request offline. Needed next: editorial/personalized radio from additional legal providers and richer audio-similarity signals. |
| Personalized recommendations | Scaffolded | YouTube Music | Home shows local "For you" recommendations by weighting favorites and recent playback across artist, album, and genre metadata while skipping just-played tracks. Every row exposes deterministic reasons such as matching favorite/recent artist, album, or genre, favorite status, unplayed status, or the recent-addition fallback; Options independently enables or disables favorite-derived and history-derived ranking signals, persists both choices, and falls back to recent additions when neither is enabled. The track-only API remains compatible. Needed next: provider-backed recommendations and cross-device personalization. |
| Similar artists/albums | Scaffolded | YouTube Music | Track menus open a local Similar tracks sheet that ranks playable library matches by shared artist, album, and genre, boosts favorites/play history, explains match reasons, and can play the result queue. Full local artist and album pages deterministically rank related same-type collections from shared known artist, album, and genre metadata, expose those reasons, and navigate between results. Needed next: provider-backed artist/album pages, richer similarity graphs, collaborative/provider recommendation signals, and remote metadata enrichment where legal. |
| Concert alerts / artist updates | Roadmap | YouTube Music | Official/event provider integration. |

### Playlists And Queue Management

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Current queue playback | Done | All music apps | Cross-device queue sync and richer queue actions. |
| Manual playlists | Done | All music apps | Persisted folders, move-to-folder actions, All/Unfiled folder filtering, local artwork picker, generated artwork collages, and sync. |
| Smart playlists | Scaffolded | Namida, desktop players | Built-in dynamic playlists plus persisted user-created smart rules are implemented for metadata/lyrics search text, exact source/artist/album/genre, favorites-only, minimum/maximum duration, minimum play count, minimum days since last play, sort mode, and result limit. The editor supports nested Match all/Match any rule groups with shared persistence/UI limits of 25 groups per level, 50 rules per group, and eight group levels, plus editable private/web artwork and artwork fallbacks from matching tracks; private artwork is stripped from portable documents. A phone-width HomeScreen test proves recursive creation, evaluation, persistence, lifecycle safety, layout, and disabled controls at every bound. Bounded AetherTune import links safely copy and import nested dynamic queries without private artwork; the listener handles each supported native `aethertune://` link once and focuses Playlists after import. Needed next: sync and server/public sharing. |
| Playlist artwork | Scaffolded | YouTube Music, Namida, RiMusic | Manual playlist menu accepts a validated local PNG/JPEG/GIF/WebP image copied into private app storage, or an http/https artwork URL, and can remove either. Explicit artwork overrides a generated collage from up to four ordered playlist tracks; empty playlists retain a stable fallback. Explicit manual artwork now has a non-destructive square crop editor with bounded pan, 1x-3x zoom, reset, live preview, persistence, and portable crop metadata; replacing or removing the artwork resets the crop. Private local image paths are stripped from backup, portable playlist, and sync documents while their harmless crop settings remain; web artwork remains portable. Needed next: image cache policy and cross-device artwork-file sync. |
| Collaborative/shared playlists | Roadmap | YouTube Music | Server sync and permissions. |
| Playlist import/export | Done | Spotube, YouTube Music migration needs | JSON, M3U, and CSV native file import/export plus view/paste fallbacks are implemented for tracks already in the local library. Files are strict UTF-8 text and use safe playlist filenames. |
| Find in playlist | Done | YouTube Music | Search box filters playlist tracks by title, artist, album, or locally saved lyrics while preserving playlist order. |
| Save queue as playlist | Done | Common player feature | Bulk queue actions and sync. |
| Radio queue generation | Scaffolded | YouTube Music, RiMusic | Local queue builders cover individual playable track seeds plus artist/album collections. Collection radio picks an engagement-aware anchor, keeps remaining collection tracks ahead of related metadata matches, exposes deterministic seed/artist/album/genre/favorite/history reasons, starts playback, and saves the generated queue as an editable playlist. The provider-neutral radio extension now builds bounded remote queues for Jellyfin and Navidrome/Subsonic track, artist, and album seeds with duplicate protection, runtime stream resolution, guarded retry, and offline denial. Needed next: more legal recommendation adapters, provider-personalized ordering, and richer audio-similarity signals. |

### Lyrics

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Plain lyrics | Done | YouTube Music, InnerTune, Namida | Manual plain lyrics, UTF-8 `.txt` import, matching `.txt` folder-scan sidecar import, LRCLIB plain-result selection, attributed local persistence, native `.txt` save dialogs, copyable export text, local search, and attributed share excerpts are implemented. Needed next: richer display and more provider adapters. |
| Synced LRC lyrics | Done | InnerTune, RiMusic, Namida | LRC parser, timestamped editor preview, playback-linked now-playing highlighting/autoscroll, UTF-8 `.lrc` import, matching `.lrc` folder-scan sidecar import, LRCLIB synced-result selection, attributed local persistence, native `.lrc` save dialogs, and copyable export text are implemented. |
| Lyrics search | Done | YouTube Music | The editor opens an explicit LRCLIB search sheet, discloses the contacted domain and metadata sent, uses the documented unauthenticated `/api/search` endpoint with an identifying User-Agent, ranks up to 20 plain/synced results locally by title/artist/album/duration, handles loading/empty/error/retry/instrumental states, blocks network search in offline mode, and stores only the selected result with provider ID/name/record/URL attribution. Provider, widget, model, store, restart, backup, merge, and share paths are covered; upstream pagination and more provider adapters remain future extensions. |
| Offline lyrics cache | Scaffolded | InnerTune, Namida | LRCLIB search responses are cached locally by normalized search query in timestamped envelopes. Offline mode invokes a cache-only provider path that cannot contact LRCLIB and reports missing or expired results. Options persistently selects a bounded 1/7/30/90/180-day retention window and can clear every cached lyrics search. Needed next: additional legal lyrics providers. |
| Lyrics sharing cards | Scaffolded | YouTube Music | Lyrics editor and now-playing lyrics sheets can copy a bounded AetherTune lyrics excerpt from saved or draft plain/LRC lyrics, stripping LRC timestamps and limiting shared lines by default. A shared range picker selects one to eight visible lyric lines for copied/native-share text while clamping malformed ranges safely; the editor can also preview and save a fixed-size PNG lyric card through the native file dialog. Formatter tests cover plain, synced, bounded, selected, and clamped output. Needed next: artwork backgrounds, rights-aware provider permissions, and deep links. |
| Manual lyrics import/edit | Scaffolded | Local library players | Manual editor imports UTF-8 `.txt` and `.lrc` files through the platform file picker, folder scans automatically associate matching basename `.lrc` or `.txt` sidecars without overwriting existing saved lyrics, and bounded embedded MP3 ID3v2 `USLT`/legacy `ULT`, FLAC/Ogg/Opus `LYRICS`/`UNSYNCEDLYRICS`, and M4A `©lyr` lyrics fill tracks that have neither a saved lyric nor sidecar. The editor previews parsed synced LRC lines, saves/deletes per-track lyrics, can select attributed plain/synced LRCLIB results, saves full TXT/LRC export files with suggested filenames, copies export text, and copies bounded attributed share excerpts. Needed next: more embedded tag formats and batch matching. |

### Offline, Cache, And Downloads

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Local offline playback | Done | Local music apps | Better file indexing. |
| Stream cache | Scaffolded | InnerTune, RiMusic, YouTube Music | Provider-approved cache requests can be queued, paused/resumed, managed, downloaded into private app storage for direct HTTP(S) media URLs, actively cancelled while foreground processing, resumed from retained private `.part` files with HTTP Range requests on retry, checksum-verified after write, stored with byte-count/checksum metadata, exported to a user-chosen folder after verification, measured, trimmed/cleared to configurable app/provider size limits, automatically evicted after cache writes when over limit, and replayed as local files. Background fetch jobs remain. |
| Download queue | Scaffolded | YouTube Music, NewPipe | Sources tab queues legal downloads only after `OfflineMediaPolicy` allows them, persists the queue with cache metadata and paused state, exposes remove/clear/cache/export/pause/resume management in Options, actively stops foreground requests when they are paused or removed, materializes direct URLs into checksum-verified private storage with HTTP Range retry resume, can export verified cached files to a user-chosen folder, and can evict private cached files. Opt-in foreground processing handles one eligible item at a time while the app is open. OS-background jobs and system Downloads integration remain. |
| Cache size limits | Done | All offline clients | Options can measure private cache usage, set a persisted 50-51200 MB private cache limit, set persisted per-provider quotas for queued providers, trim private cache files to the app limit, clear cached media, automatically evict the oldest private cached files after successful cache writes when usage exceeds a provider quota or the app limit, and preserve limits through backup/restore. | More provider-specific default policies. |
| Per-provider offline policy | Done | Spotube, Grayjay | `OfflineMediaPolicy` allows local files, permits Podcast RSS and Internet Archive cache/download only when providers declare capability plus disclosure, denies live Radio Browser streams, and has unit coverage through provider contract/coordinator tests. |
| Offline mode toggle | Done | YouTube Music | Persisted Options toggle pauses network-backed Sources searches, feed refreshes, Sources stream playback actions, and player-wide saved URL stream playback; backup/restore preserves it, Library can filter to local offline-ready files, and Options exposes the offline queue/cache manager with usage/app-provider quota eviction controls, HTTP Range retry resume for direct URL cache writes, automatic post-cache pressure eviction, and bounded sequential foreground processing of approved queued work. Needed next: background download jobs. |
| Partial/resumable downloads | Scaffolded | NewPipe-style clients | Default direct HTTP(S) cache writes resume from existing private `.part` files with Range requests and still verify the final byte count/checksum; queued requests can be paused before or during foreground processing, which stops the request without discarding its partial bytes. Needed next: background workers, provider-supplied checksum manifests, and system Downloads integration. |

### Providers And Sources

| Source/provider | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Local files | Done | Namida, Musify | Recursive folder scanner, filename metadata parser, matching `.lrc`/`.txt` lyric sidecar association, basic ID3v1 MP3 title/artist/album tag parser, basic ID3v2 MP3 title/artist/album/genre text-frame and APIC/PIC artwork parser, basic FLAC Vorbis comment title/artist/album/genre and picture artwork parser, basic M4A title/artist/album/genre atom plus `covr` artwork parser, basic WAV RIFF INFO title/artist/album/genre parser, persisted watched-folder rescan, private file/web artwork overrides, and standard M4A PNG/JPEG embedded-artwork writing with validated front-loaded-offset repair are implemented; richer tag parsing and artwork cropping remain. |
| Demo provider | Done | Provider template | Developer docs and test fixture provider. |
| Jellyfin | Scaffolded | Self-hosted music users | Sources can add/edit/test/remove HTTPS-by-default Jellyfin accounts, store API keys in the platform credential vault, activate authenticated paginated audio search, and open paginated Artists, Albums, and Playlists tabs. The account menu has confirmed test-before-replace API-key rotation with vault rollback, redacted errors, artwork invalidation, and active queue re-resolution. Artist-to-album and album/playlist-to-track drill-down supports filtering, play/save/offline actions, remote playlist mutation, and track/artist/album radio through Jellyfin's documented song, artist, and album Instant Mix endpoints. Safe image IDs/tags persist with metadata; validated artwork renders through bounded in-memory/private-file caches in app and system media surfaces without exposing credential URLs. Exact read/write/search/page/radio endpoints and parameters, duplicate/order preservation, input/no-op rules, secure store/cache/serialization/rotation, phone/desktop flows, failure/retry, and offline zero-request behavior have tests. Portable snapshots retain safe saved metadata only; account secrets and remote playlist state stay device-local. Needed next: account migration and remote-provider coordination. |
| Navidrome/Subsonic | Scaffolded | Self-hosted music users | Sources can add/edit/test/remove HTTPS-by-default Navidrome/Subsonic accounts, store passwords in the platform credential vault, activate `ping.view` testing plus paginated `search3.view` search, browse documented artist/album/playlist endpoints with paginated albums, and continue Home discovery shelves with documented `getAlbumList2.view` size/offset requests. The account menu has confirmed test-before-replace password rotation with vault rollback, redacted errors, artwork invalidation, and active queue re-resolution. Every request uses random-salt `t=md5(password+salt)` instead of reversible `p=enc:` authentication. Track/album radio uses `getSimilarSongs.view`; artist radio uses `getSimilarSongs2.view`, which Navidrome supports when Last.fm integration is configured. The separately declared playlist-editing capability exposes create/rename/delete/add/remove/reorder with ordered duplicate preservation. Credential-safe artwork renders through bounded in-memory/private-file caches in app and system media surfaces. Exact browse/discovery/radio endpoints/parameters/parsers, fresh salts, guarded retry, queue dedupe, and offline zero-request behavior have tests. Portable snapshots retain safe saved metadata only; account secrets and remote playlist state stay device-local. Needed next: account migration and remote-provider coordination. |
| Podcast RSS | Scaffolded | YouTube Music podcasts, NewPipe | RSS parser/provider, persisted feed subscriptions with up to 100 cached episodes each, OPML import/export, refresh status/stale policy, offline episode listing, playback, saved episode progress/resume, library save, backup/restore and same-feed sync merge, cache/download policy declarations, queued offline requests, checksum-verified private direct-enclosure caching with HTTP Range retry resume, and app/provider quota private cache eviction are implemented; background download jobs remain. |
| Radio Browser / internet radio | Scaffolded | Radio apps | Station search with country/language/tag/codec/bitrate filters, offset-based continuation with duplicate protection and retry-safe retained results, public API mirror discovery, fallback mirror behavior, dedicated station detail pages, selected stream validation, playback, station click accounting, library save, provider parser, playable station model, and live-stream cache/download denial are implemented; deeper codec probing and retry/backoff policy remain. |
| Internet Archive | Scaffolded | ArchiveTune | Public audio metadata search, collection/subject/creator/year filters, facet suggestion chips, total-aware paginated continuation with retry-safe retained results, dedicated item detail pages with collection/subject/license metadata and all playable files, Sources-tab play/save/cache/download queue, checksum-verified private direct-file caching with HTTP Range retry resume, app/provider quota private cache eviction, cache/download policy declarations, and provider/cache/detail tests are implemented; background download jobs remain. |
| Spotify metadata | Blocked / official-only | Spotube | Official API metadata only; playback must be legal/user-authorized. |
| YouTube / YouTube Music | Blocked / official-only | YouTube Music, InnerTune, NewPipe family | Official API, embeds, or user-provided legal URLs only; no private API scraping. |
| SoundCloud or similar services | Blocked / official-only | Multi-source clients | Official API or documented public feeds only. |
| Bandcamp / artist stores | Blocked / official-only | Open music discovery | Official/public pages only where terms allow. |
| User-added custom provider plugins | Roadmap | Echo Music, Bloomee Tunes, Grayjay | Plugin SDK, packaging, signing, sandbox, and provider contract test suite. |

### Video And Media Browsing

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Music video playback | Roadmap | YouTube Music, NewPipe, LibreTube | Legal video provider, player UI, PiP support. |
| Audio/video toggle | Blocked / official-only | YouTube Music | Only where provider terms allow. |
| Picture-in-picture | Roadmap | YouTube Music, NewPipe | Platform PiP APIs and tests. |
| Captions/subtitles | Roadmap | YouTube/NewPipe-style apps | Caption parser/provider support. |
| Chapters | Scaffolded | YouTube/NewPipe-style apps | Saved tracks carry normalized, portable chapter markers. Full Now Playing renders proportional marker ticks below the seek slider, shows the active marker, expands to a timestamped seek list, and provides a validated timestamp/title editor for the saved current track. Legal Podcast RSS feeds propagate inline Podlove Simple Chapters and same-origin Podcasting 2.0 chapter JSON documents into playable episodes; external chapter domains are never requested. Model, provider, store, and widget tests cover parsing, privacy boundaries, timeline rendering, persistence, active selection, and seek behavior. Needed next: an explicit user-approved policy for third-party chapter hosts. |
| Sponsor/segment skipping | Not included by default | Video clients | Could support user-owned/open segment data; no invasive tracking. |
| Channel/creator pages | Roadmap | Grayjay, NewPipe, LibreTube | Multi-source creator model. |
| Video comments | Not included by default | YouTube apps | High moderation/privacy cost; official APIs only if added. |

### Social, Subscriptions, And Multi-Source Feeds

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Provider subscriptions | Scaffolded | Grayjay, NewPipe, LibreTube | Podcast RSS feed subscriptions are persisted and removable; up to 100 cached episodes per feed, including unsaved episodes, appear in a dedicated Home **Subscribed episodes** section and remain browseable offline. Backup and same-feed sync merge retain those catalogs. Creator/channel subscriptions for video and multi-source feeds remain. |
| Creator/channel following | Scaffolded | Grayjay, YouTube Music | Users can follow/unfollow normalized local library artist names from a full artist page. Follows persist in device storage, backups, and library sync snapshots; Home shows newest local additions from followed artists. This is not a remote provider/channel subscription or a multi-source creator identity model. |
| Multi-source unified feed | Scaffolded | Grayjay | Home now merges newest local additions by followed artists with cached episodes from RSS subscriptions, de-duplicates matching track IDs, ranks newest-first, and filters All/Artists/Podcasts. Remote creator/channel feeds and provider ranking remain to add. |
| Public profile | Roadmap | YouTube Music | Optional server account and privacy controls. |
| Taste-match/shared mixes | Roadmap | YouTube Music | Server-side sharing and opt-in profile model. |
| Share track/album/playlist | Scaffolded | All music apps | Track menus, artist/album pages, genre/source/folder browse sheets, and playlist menus copy local AetherTune share text for tracks, groups, playlists, and lyrics; local file paths are redacted. Each successful copy confirmation now includes a native platform Share action using the same text, with an anchored iPad/macOS presentation origin and unavailable/error states. Manual and smart playlists can copy bounded `aethertune://playlist` or `aethertune://smart-playlist` import links that embed sync-safe documents. The app now queues cold-start or foreground custom URLs until the library loads, imports each supported link at most once per session through the existing bounded validators, opens Playlists, and reports success or validation failures. Pasted-link import matches local tracks without carrying device paths, stream URLs, or private artwork. Now Playing previews and saves a privacy-safe artwork track card as PNG; artist/album pages and playlist menus preview and save PNG cards using representative collection artwork, explicit playlist artwork, or generated playlist collages; monthly/yearly listening recaps render and save as PNG cards. Native scheme registration and generated-wrapper checks are covered separately; server-backed public sharing remains. |
| Comments/community posts | Not included by default | YouTube-style platforms | Moderation burden; official APIs only if ever supported. |

### Platform Integrations

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Android notification controls | Scaffolded | YouTube Music, InnerTune, Namida | `SystemMediaPlaybackEngine` publishes queue metadata/artwork/playback state and handles transport, seek, repeat, and shuffle; authenticated provider artwork is exposed only through bounded private local files; bootstrap adds and validates wake-lock/foreground-media permissions, `AudioService`, and `MediaButtonReceiver`; CI compiles an APK and unit-tests the handler contract. Needed next: emulator/physical notification lifecycle tests. |
| Android Auto | Roadmap | YouTube Music, local music players | Media browser service and car emulator/device tests. |
| Android widgets / shortcuts | Scaffolded | Music apps | Bootstrap generates and validates a launcher-visible Android home-screen widget with previous, play/pause, next, and app-open actions plus static launcher Previous, Play/Pause, and Next shortcuts. Both paths deliver transport actions to the existing audio_service media-button receiver. The system-media engine publishes the current title, artist, play state, and bounded position/duration over a narrow internal Android channel; the widget persists that display state, refreshes every installed instance, swaps its play/pause icon, and renders one-second-throttled playback progress when duration is known. Local file-scheme cover artwork is decoded once per artwork change into a bounded 192 px bitmap and rendered in the widget; remote, data, and credential-bearing artwork URLs never cross the Android channel. Generator, bridge-privacy, and engine-contract tests run before the Android APK compile. Needed next: user-pinned shortcuts and emulator/physical launcher fixtures. |
| Android scoped storage polish | Roadmap | Local library apps | Permissions flow and folder grants. |
| iOS Control Center | Scaffolded | YouTube Music | `audio_service` receives queue metadata/artwork/playback state and transport/seek/repeat/shuffle callbacks, with authenticated provider artwork supplied through credential-free private local files; CI compiles an unsigned iOS app. Needed next: physical Control Center fixtures. |
| iOS lock screen metadata | Scaffolded | YouTube Music | Current title, artist, album, artwork, duration, position, queue index, and playback state are published as `MediaItem`/`PlaybackState`; physical lock-screen verification remains. |
| iOS background audio | Scaffolded | YouTube Music | Startup configures a music `AudioSession`, bootstrap adds `UIBackgroundModes: audio`, and the unsigned iOS CI build validates native integration. Needed next: physical interruption/route/background lifecycle tests and App Store review notes. |
| CarPlay | Roadmap | YouTube Music | Apple entitlement and CarPlay UI. |
| Linux desktop packaging | Roadmap | Desktop players | AppImage/Flatpak/deb workflow. |
| Windows packaging | Roadmap | Desktop players | MSIX/zip installer and signing. |
| macOS packaging | Roadmap | Desktop players | Notarization, dmg/pkg, signing. |
| Desktop tray / menu bar | Scaffolded | Desktop players | Linux, macOS, and Windows initialize a native tray/menu-bar icon generated locally at runtime. The menu shows/focuses the app, routes previous/play-pause/next transport actions, and closes the app on Quit. Options persist a desktop-local minimize-to-tray-on-close policy that keeps playback alive until Quit is selected. The Windows icon is a locally generated PNG embedded in a valid ICO container; no network or track artwork is used. Command routing, close-policy selection, platform eligibility, and ICO header/payload tests are covered. Needed next: physical desktop-host fixtures and user-configurable menu actions. |
| Global hotkeys | Scaffolded | Desktop players | Windows, macOS, and Linux register system-wide media play/pause, previous, and next keys through the native hotkey manager. Each key registration is isolated so an OS-reserved media key does not disable the remaining transport keys, and shutdown unregisters only AetherTune's keys. Controller tests cover routing, failure isolation, cleanup, and platform eligibility; physical-host conflict and permissions fixtures remain. |
| Cast / Chromecast | Roadmap | YouTube Music | Cast integration where legal/provider-supported. |
| Wear OS / watch controls | Roadmap | YouTube Music | Companion app or media session support. |

### Server, Sync, And Accounts

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Server health endpoint | Done | Production services | Deployment checks. |
| Server info endpoint | Done | Client compatibility | Versioned capabilities. |
| Server catalog endpoint | Done | API foundation | Persistence and auth. |
| Server executable compile | Done | Release readiness and release workflow server artifacts. | Deployment workflow. |
| Authentication | Scaffolded | Sync services | Existing static raw/hash-only bearer configuration now accepts several device credentials for one account. The server also keeps an opt-in durable account registry, issues 256-bit random device tokens exactly once through operations-authenticated APIs, stores only SHA-256 digests, supports atomic selected-token rotation and independent revocation, bounds account/token counts and request sizes, compares digests in constant time, and fails managed administration closed without the separate operations token. The mobile client already requires HTTPS by default and keeps its sync token in platform-secure storage. Registry restart, shared-account, rotation, revocation, invalid-input, log-redaction, and API tests are implemented. Needed next: public registration or external identity/OAuth integration, expiry policy, rate limiting at the deployment edge, and physical credential-vault tests. |
| User profiles | Scaffolded | YouTube Music | Managed sync accounts persist a stable account ID, display name, creation time, and non-secret per-device token metadata. An authenticated profile endpoint identifies only the current account/device, while the operations endpoint lists profile/device metadata without raw tokens or digests. Authenticated managed devices can atomically rename the shared account and their own unique device label without changing token identity; static accounts cannot mutate server state. The Flutter client validates this capability during test-and-save, treats `404` as an older compatible server, defaults a missing edit capability to false, persists only selected non-secret fields with rollback, displays managed or static identity in Options, refreshes it, and exposes a bounded account/device editor only when advertised. A validated edit response must preserve account and token IDs, and the edited device name also updates local upload attribution. Several independently revocable devices can own the same sync snapshot. Registry/API/client/store/widget tests cover validation, idempotency, durable restart, forced persistence rollback, duplicate names, static rejection, compatibility, persistence, refresh/edit, identity substitution rejection, removal, secret exclusion, offline controls, and phone layout. Needed next: optional avatars, privacy controls for any public profile, and an account recovery flow. |
| Library sync | Done | YouTube Music / multi-device needs | The optional Dart server accepts authenticated `GET`/`PUT`/`DELETE /api/v1/sync/library` snapshots with size checks, checksums, durable latest-revision storage, revisioned reset tombstones, and optimistic conflict responses. Separate static or managed device tokens can resolve to one account and therefore one snapshot. The client requires HTTPS unless the user explicitly permits HTTP, stores its token securely, exposes manual upload/download, confirmed remote-only reset, and conflict resolution, and can opt into foreground uploads at most every 15 minutes. A remote reset disables automatic uploads so it cannot recreate the cleared copy. On an explicit conflict, **Merge both** fetches the current server revision, unions independent library records, combines matching playlist memberships in local order, selects newer progress/lyrics timestamps, keeps local scalar preferences and same-ID metadata, and uploads the merged result against the fetched revision. Automatic uploads never download, overwrite, or merge; background scheduling and per-item sync APIs remain roadmap work. |
| Playback position sync | Scaffolded | Podcasts/video apps | Local podcast episode progress/resume is implemented, and the manual authenticated library snapshot carries valid progress entries across devices with the rest of the portable library. Automatic background sync, per-item conflict merge, and video-specific state remain. |
| Playlist sync | Scaffolded | Music services | Manual playlists, ordered track IDs, and safe remote artwork URIs are included in the authenticated portable library snapshot and restore across devices. Automatic per-playlist merge, collaboration, provider credentials, and a dedicated server playlist API remain. |
| Provider credential vault | Scaffolded | Self-hosted/official APIs | Jellyfin API keys and Navidrome/Subsonic passwords use `flutter_secure_storage` across Android, iOS, Linux, macOS, and Windows; account metadata and safe artwork IDs are separate, secrets and credential-bearing media/artwork URLs are excluded from queue/library JSON and backups, Subsonic requests derive per-request salted tokens, and confirmed test-before-replace rotation rolls back failed vault writes before invalidating artwork and re-resolving active queues. Deletion removes vault entries and provider caches, Android backup is disabled, Apple keychain entitlements are generated, and Linux CI/release installs libsecret. Needed next: biometric policy choices, migration/versioning, and physical-device keychain/keystore/credential-manager tests. |
| Admin/ops endpoints | Done | Server deployments | `GET /health` supports deployment checks. `GET /api/v1/metrics` reports aggregate process uptime, request count, and sync configuration state without users, paths, addresses, or payloads; values reset on server restart and support a distinct raw or hash-only operations bearer token with constant-time verification. The executable emits JSON request logs with only timestamp, method, normalized route, status, and duration; logger failures never affect a response and secrets are excluded by construction. External log retention and dashboards remain deployment concerns. |
| Federation/self-hosting docs | Scaffolded | Open-source server users | Docker/Compose defaults to loopback binding; deployment docs provide Caddy HTTPS, native systemd, managed per-device token bootstrap/rotation/revocation, static token hashing, auth-registry/snapshot backup, update, health verification, and client setup paths. Needed next: automated certificate/reverse-proxy provisioning and multi-host federation. |

### UI, UX, Accessibility, And Customization

| Feature | Status | Inspired by | Needed to add |
|---|---:|---|---|
| Material 3 shell | Done | Music You, modern Android apps | Full design system. |
| Mini player / full player | Done | All music apps | The compact player now switches to a phone-safe control set below 720 px and opens a responsive full Now Playing route. The full route provides large artwork, live seek/elapsed/remaining time, queue position, favorite, lyrics, queue, shuffle, repeat, previous/play-pause/next controls, and left/right artwork swipe navigation; widget tests cover the full control contract, automatic queue transition, and a 390 px no-overflow mini player. More secondary actions can be added with future provider features. |
| Desktop responsive layout | Scaffolded | Desktop players | The Flutter shell switches from bottom navigation to a labeled, scrollable `NavigationRail` at desktop widths while preserving the player bar and tab state across Linux, macOS, and Windows builds; focused-window media play/pause, next, and previous keys plus Ctrl+K transport toggle are bound at the app shell. At desktop widths, Ctrl+1 through Ctrl+6 select Home, Library, Playlists, History, Sources, and Options, while Alt+Left/Right cycle tabs within an ordered focus-traversal group. Wide desktops add a persisted, 260-480px resizable queue split pane with selection, removal, and reordering; Options can retain the app in the system tray when its window closes and choose Comfortable or Compact density without altering phone/tablet layouts. Store, theme-mapping, and desktop Options workflow tests cover persistence and platform boundaries. Needed next: host-specific density polish. |
| Themes | Scaffolded | Music You, RiMusic | Options exposes persisted System, Light, Dark, AMOLED, and System colors or named accent swatch choices; the app shell switches `ThemeMode`, applies named swatches as explicit Material 3 overrides, and uses the platform dynamic light/dark schemes for System colors when available, with a stable indigo fallback where unavailable. Backups preserve theme/accent preferences; store/theme tests cover persistence, restore, dynamic-scheme selection, fallback, and override behavior. Needed next: per-platform polish. |
| AMOLED theme | Scaffolded | Android music apps | AMOLED preference forces dark mode and uses black scaffold/canvas/navigation surfaces. Needed next: full component contrast audit and per-screen black-surface tuning. |
| Artwork-dominant player | Scaffolded | Namida, YouTube Music | The responsive full Now Playing route gives artwork up to 480 px the primary mobile/desktop visual position and keeps controls unframed beside or below it; artwork swipes navigate the queue. A bounded, deterministic RGBA sampler now derives a subtle animated player-surface tint from a 192 px public/local artwork decode while preserving the active Material foreground contrast. Credential-backed self-hosted artwork deliberately retains the normal themed surface so no secondary credential-aware image request is introduced. Palette-selection, transparent-artwork fallback, surface tint, and full-player widget behavior have focused tests. Animated shared-element transitions and visualizer integration remain. |
| Visualizer | Scaffolded | Namida-style polish | Android Now Playing offers an explicit opt-in visualizer. It waits for a live `just_audio` Android audio-session ID, requests Android's required record-audio permission only after the user enables it, attaches the platform `Visualizer` to AetherTune's own session, emits bounded 16-band FFT data, and releases the native effect when disabled or when Now Playing closes. Unsupported platforms show no control. Bridge normalization and generated Android wrapper checks are covered; physical-device FFT/acoustic fixtures and equivalent non-Android backends remain. |
| Accessibility pass | Scaffolded | Required quality gate | App-wide Material controls retain keyboard traversal and tooltips; primary navigation uses labeled destinations, playback volume/progress sliders provide spoken values, charts and listening heatmaps expose semantic summaries, and Now Playing artwork exposes screen-reader next/previous queue actions in addition to touch swipes. Focused widget tests prove playback semantics and control behavior. Needed next: a per-screen screen-reader and contrast audit on physical Android, iOS, Windows, macOS, and Linux hosts. |
| Localization | Scaffolded | Global music apps | Generated Flutter ARB localization is enabled with English, Turkish, and Arabic catalogs. Options now persists an explicit System, English, Turkish, or Arabic language override through device storage and portable backups; System remains the compatible default. The application title, responsive phone/desktop navigation, language control, and first-run onboarding are localized; Arabic uses Flutter's RTL locale directionality. Store and widget tests cover persistence, backup restore, locale mapping, Turkish strings, and Arabic RTL layout. Needed next: translate the remaining feature UI, broaden language coverage, and localize dates, number formats, exports, provider states, and accessibility copy. |
| Onboarding | Scaffolded | Consumer apps | A persistent first-run screen offers a local-library, legal-sources, or privacy-first starting point, routes into the corresponding existing tab, can be skipped, and can be replayed from Options. Needed next: platform permission education, import progress, and guided self-hosted connection setup. |
| Empty/error/loading states | Scaffolded | Required product quality | Standard state components and tests. |

### Privacy, Safety, And Legal Boundaries

| Feature/policy | Status | Notes |
|---|---:|---|
| No telemetry by default | Done | Keep analytics out unless explicit opt-in is ever added. |
| No DRM bypass | Not included | Will not be implemented. |
| No credential stealing | Not included | Will not be implemented. |
| No paid-service cloning | Not included | Will not be implemented. |
| No private API scraping | Not included | Use official/documented APIs, open feeds, or user-owned servers. |
| Provider permission model | Done | Providers declare capabilities and privacy-sensitive behaviors through `MusicSourceProvider`; Sources tab displays the disclosure and `OfflineMediaPolicy` gates cache/download eligibility. |
| Network request disclosure | Done | `ProviderPrivacyDisclosure` lists contacted domains and data sent for each adapter. |
| Pause listening history | Done | History and Options expose a persisted toggle that stops new playback-history and resume-progress writes; backup/restore preserves the preference. |
| Delete listening history entries | Done | The History tab lists individual play events for the selected range and can remove one event without clearing all history. |
| Secure token storage | Scaffolded | Implemented for static Jellyfin and Navidrome/Subsonic credentials through platform-secure storage with HTTPS default, explicit insecure-HTTP consent, redacted errors, no authenticated URL persistence, confirmed atomic rotation with rollback, and random-salt Subsonic request tokens that avoid transmitting the reversible encoded password. OAuth refresh tokens and physical-device storage tests remain. |
| Content policy/moderation | Roadmap | Required before public profiles, comments, or social surfaces. |

### Testing, Release, And Maintenance

| Feature | Status | Needed to add |
|---|---:|---|
| Flutter analyze/test CI | Done | Broader widget/integration tests. |
| Desktop build CI | Done | Release-mode packaging and smoke tests. |
| Server analyze/test/compile CI | Done | API integration tests and load tests. |
| Release artifact workflow | Done | Tag/manual workflow builds APK/AAB, desktop archives, server binaries, and a reproducible resolved-dependency inventory plus CycloneDX 1.5 SBOM for the client and server. | Signing, notarization, installers, and store packaging. |
| Signed releases | Roadmap | Android signing, macOS notarization, Windows signing. |
| SBOM / dependency audit | Scaffolded | CI and tag/manual release runs resolve each Dart graph, publish its raw `dart pub deps --json` inventory and a deterministic CycloneDX 1.5 SBOM, then regenerate it byte-for-byte as a proof gate. Dependabot is configured to maintain both Pub manifests, the server Docker/Compose definitions, and GitHub Actions weekly. Repository-enabled Dependabot alerts/security updates can act on the manifests; an explicit license-policy or independent advisory scan remains to add. |
| Golden UI tests | Roadmap | Stable screenshots for key views. |
| Provider contract test suite | Scaffolded | Shared capability/disclosure tests cover Demo, Podcast RSS, Radio Browser, Internet Archive, Jellyfin, and Navidrome/Subsonic; search tests cover opaque cursor coordination, fallback limits, cross-page ranking/deduplication, failed-cursor retry, non-progressing cursor rejection, exact Archive page/rows, Radio offset/limit, Jellyfin StartIndex/total, Subsonic songOffset/songCount, and retained-results Sources UI. Self-hosted tests additionally cover secure metadata/secret separation, connection testing, rejected rotation, failed-vault-write rollback, old/new secret redaction, successful provider reconstruction, memory/private artwork invalidation, ephemeral stream/artwork URL stripping, position-preserving active queue re-resolution, unresolved-track stopping, restart reconstruction, removal, exact browse/page/detail/artwork/playlist-write/radio endpoints and parameters, page bounds/totals/offsets, retained catalog continuation results and retry, input validation/no-op append, ordered duplicate IDs, fresh mutation salts, guarded mutation/radio failure state, radio queue ordering/deduplication, system-media publication, phone/desktop navigation, play/save/radio/create/rename/delete/add/move/remove actions, offline zero-request behavior, and native wrapper requirements. Dedicated LRCLIB tests cover its separate lyrics contract. Needed next: reusable fixture package, live opt-in integration fixtures, and CI reporting per adapter. |
| E2E smoke test | Scaffolded | A deterministic widget smoke flow launches the real `AetherTuneApp` provider tree, completes onboarding, persists a local-library entry, renders it in Library, and queues it through `PlayerController` using a no-platform in-memory audio engine. Physical file-picker import and device-audio smoke fixtures remain to add. |
| Crash/error reporting | Scaffolded | Flutter framework and uncaught platform-dispatcher errors are captured into a bounded, redacted device-local diagnostic log. Options shows its count and supports explicit JSON export or destructive clearing; nothing is uploaded. Store tests prove persistence, retention, and redaction of common tokens, passwords, file URLs, and URL userinfo. Remote reporting remains intentionally absent unless a future explicit opt-in policy is designed. |

## App-Inspiration Coverage Map

This table maps each named app to the AetherTune feature surface it implies. It is not a claim that the current app implements all of each app.

| App | Coverage target implied | Current status |
|---|---|---:|
| Kreate | YouTube Music-style discovery, background playback, cache, playlists, lyrics. | Roadmap |
| OpenTune | YouTube Music-style browse/search, lyrics, playlists, offline/cache. | Roadmap |
| InnerTune | YouTube Music client UX, background playback, cache, synced lyrics. | Roadmap |
| SimpMusic | Music/video source organization, online browse, player UX. | Roadmap |
| ArchiveTune | Internet Archive search, metadata, playable public files. | Scaffolded |
| Spotube | Metadata/playback provider separation, Spotify-style metadata. | Scaffolded / official-only |
| Echo Music | Extension/provider-based architecture. | Scaffolded |
| Namida | Strong local library, metadata, advanced player, stats, beautiful UX. | Roadmap |
| PipePipe | Privacy-first video/music browsing and subscriptions. | Roadmap / official-only |
| NewPipe | Video/music browsing, downloads where legal, subscriptions, background audio. | Roadmap / official-only |
| LibreTube | Privacy-first YouTube-style client concepts. | Roadmap / official-only |
| Musify | Lightweight music-first playback and local/simple online sources. | Roadmap |
| AuraMusic | Lightweight music-first UI and discovery ideas. | Roadmap |
| Bloomee Tunes | Multi-source/provider music experience. | Scaffolded |
| Gyawun Music | Lightweight mobile music UX. | Roadmap |
| Music You | Material You styling and Android-native polish. | Scaffolded |
| Muzza | Material mobile player ideas. | Roadmap |
| SoundPod | Lightweight streaming/player UX. | Roadmap |
| NouTube | Music/video source browsing. | Roadmap / official-only |
| Grayjay | Multi-source subscriptions, creator following, unified feeds. | Roadmap |
| OuterTune | YouTube Music-style client features. | Roadmap / official-only |
| ViTune | YouTube Music-style playback, cache, lyrics. | Roadmap / official-only |
| RiMusic | YouTube Music-style playback, cache, lyrics, Android polish. | Scaffolded / official-only |
| Harmony Music | Lightweight music client UX and provider ideas. | Roadmap |
| YMusic | YouTube-audio-oriented UX. | Roadmap / official-only |
| YouTube Music | Official music/video streaming UX, recommendations, radio, podcasts, downloads, profiles. | Roadmap / official-only |
| Flow | Lightweight music-first user experience. | Roadmap |
| MetroList | Clean music player/library UX. | Roadmap |

## Minimum Build Plan To Reach Real 100% Implemented Parity

1. Replace JSON preferences with a real local database and migrations.
2. Build full local library: recursive folder import, metadata scanner, metadata editing, duplicate resolver, playlists, backup/restore.
3. Extend the implemented mobile/system sessions and desktop global media keys with physical-host lifecycle fixtures, native Windows SMTC/Linux MPRIS publication, and Android Auto/CarPlay browsing where allowed.
4. Build provider SDK v1 with capability declarations, network disclosure, auth handling, and contract tests.
5. Complete legal providers: background Podcast RSS jobs, richer Radio Browser UX, automated self-hosted sync merge, and pagination/browse surfaces for additional legal providers.
6. Add official-only adapters where terms allow: Spotify metadata, YouTube/YouTube Music, SoundCloud, Bandcamp, or others.
7. Expand the current checksum-verified direct-URL offline cache with HTTP Range retry resume, queue pause/resume, and user-chosen folder export into a full cache/download manager with background jobs, in-progress cancellation, provider checksum manifests where available, and deeper system Downloads integration where legally allowed.
8. Add lyrics: plain text, synced LRC, cache, search, manual import/edit.
9. Add discovery: home feed, charts, recommendations, moods, artist/track radio, similar artists.
10. Add video surfaces only through legal providers: video player, PiP, captions, chapters, subscriptions.
11. Extend the implemented static-credential vault with OAuth token refresh, biometric policy, and migration/versioning, then build automatic sync, per-item playlist/library/playback-position merge APIs, and self-hosting docs.
12. Polish desktop: responsive layout, tray/menu bar, global hotkeys, installers, signing.
13. Harden release artifacts with signing, notarization, installers, and store-ready packaging.
14. Add accessibility, localization, golden tests, integration tests, provider tests, dependency audits, and privacy/network audits.

## Definition of "100% free/open-source"

AetherTune can be 100% free and open-source because its code, docs, and license are open. That is different from claiming 100% parity with proprietary or unofficial apps. Source adapters must also be free/open-source and legal before they can be merged into the core repository.
